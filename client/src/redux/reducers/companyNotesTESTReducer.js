const defaultData = {
  companyName: '',
  companyDetails: '',
  notes: '',
};

export default function companyNotesTESTReducer(state = defaultData, action) {
  switch (action.type) {
    default:
      return state;
  }
}
